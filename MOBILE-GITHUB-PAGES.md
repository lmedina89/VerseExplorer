# v0.1.0.5D Safari/GitHub Pages cache chain

Changed runtime chain: `index/main → app → threeRenderer/surfaceGenerator/surfaceProfiles → surfaceWorld`. Changed edges use `ue0105d`; unchanged deep dependencies retain their prior stable tags. This prevents iPhone Safari from combining 5C.1 landing modules with 5D surface profiles.

Acceptance after upload: verify the visible badge reads `v0.1.0.5D`, then test one landing/takeoff on Mars, Europa, Titan and Triton.
