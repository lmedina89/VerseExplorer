{
  "name": "universe-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Exploration-focused branch of Universe Lab v0.1.5.5 preserving the proven 3D simulation, spacecraft, FRAME travel, cockpit, landing and surface systems while reducing default UI overhead.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
